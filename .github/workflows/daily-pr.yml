name: Daily Auto Commit & PR

env:
  TARGET_BRANCH: main

on:
  schedule:
    # Runs every day at 14:40 GMT+7 (07:40 UTC)
    - cron: "40 7 * * *"
  workflow_dispatch: # Allows manual trigger from GitHub UI

permissions:
  contents: write
  pull-requests: write

jobs:
  daily-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 0

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create branch name with today's date
        id: branch
        run: |
          BRANCH_NAME="auto-update/$(date +'%Y-%m-%d')"
          echo "name=$BRANCH_NAME" >> "$GITHUB_OUTPUT"

      - name: Delete existing remote branch if present
        run: |
          git push origin --delete "${{ steps.branch.outputs.name }}" || true

      - name: Create or switch to the daily branch
        run: |
          git checkout -b "${{ steps.branch.outputs.name }}"

      - name: Make your daily changes
        run: |
          TIMESTAMP=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
          cat > workflow.md << EOF
          # Daily Workflow Update

          Last updated: ${TIMESTAMP}
          EOF

      - name: Check for actual changes
        id: changes
        run: |
          if git diff --quiet && git diff --staged --quiet; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push changes
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git add -A
          git commit -m "chore: daily auto-update $(date +'%Y-%m-%d')"
          git push origin "${{ steps.branch.outputs.name }}"

      - name: Create pull request
        if: steps.changes.outputs.has_changes == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --title "chore: daily auto-update $(date +'%Y-%m-%d')" \
            --body "Automated daily update generated on $(date -u +'%Y-%m-%dT%H:%M:%SZ').

          This PR was created automatically by the **Daily Auto Commit & PR** workflow.

          ## What changed
          - Updated \`workflow.md\` with today's timestamp.

          > Review and merge when ready." \
            --base "${{ env.TARGET_BRANCH }}" \
            --head "${{ steps.branch.outputs.name }}"

      - name: Enable auto-delete branch after PR merge
        if: steps.changes.outputs.has_changes == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr merge "${{ steps.branch.outputs.name }}" --delete-branch --auto --squash || true
